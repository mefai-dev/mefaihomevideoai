# Cloudflare Turnstile public site key (safe to expose in build output)
VITE_SUPERBCS_TURNSTILE_SITE_KEY=CHANGE_ME

# Base URL of the backend API (see /api folder)
VITE_SUPERBCS_API_BASE=https://api.example.com
